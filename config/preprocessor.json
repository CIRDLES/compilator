{
	"clickOnChildren": {
		"schemaSelector": "$..children[?({{#each selectors}}{{#unless @first}} && {{/unless}}@.{{{@key}}}==\"{{{this}}}\"{{/each}})]",
		"schemaProperties": ["children"],
		"unique": true
	},
	"clickOnAll": {
		"schemaSelector": "$..children[?(@.name && @.label)]"
	}
}